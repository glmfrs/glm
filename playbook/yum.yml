---
#- name: yum suppression de l'option fastestmirror
#  lineinfile:
#    path: /etc/yum/pluginconf.d/fastestmirror.conf
#    regexp: '^enabled='
#    line: enabled=0
#  ignore_errors: yes

- name: yum mise à jour de tous les paquets
  yum:
    name: '*'
    state: latest
    update_cache: yes

- name: yum installation des paquets
  yum:
    name:
      - bash-completion
      - bind-utils
      - bzip2
      - ca-certificates
      - cockpit
      - cracklib
      - curl
      - epel-release
      - git
      - haveged
      - htop
      - logrotate
      - nano
      - net-snmp-utils
      - nmap-ncat
      - rsync
      - sudo
      - telnet
      - tmux
      - unzip
      - vim-enhanced
      - wget
      - xz
    state: latest
    update_cache: yes
  ignore_errors: yes

- name: yum installation du depot epel
  yum:
    name: epel-release
    state: latest
    update_cache: yes
  ignore_errors: yes